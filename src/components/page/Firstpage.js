const Firstpage = (props) => {
  return (
    <div>
      <div>{props.nav}</div>
      <div>{props.herosection}</div>
      <div>{props.content}</div>
      <div>{props.footer}</div>
    </div>
  );
};
export default Firstpage;
